# NehoRace

משחק מירוץ בעברית. בונים "נהוראי" (קריקטורה ישראלית), בוחרים כלי רכב, ומתחרים בפארק מול חמישה יריבים בזמן שדורסים הולכי רגל.

המשחק בנוי מ-`index.html` (שלד המסכים), `styles/app.css`, ו-31 מודולי `ES modules` בתיקייה `src/`, בלי ספריות חיצוניות ובלי שלב בנייה. נקודת הכניסה היא `src/main.js`. הכל מצויר בקנבס בקוד, אין תמונות ואין נכסים חיצוניים. מפת המודולים נמצאת ב-`docs/ARCHITECTURE.md`.

## אתר חי

https://nehorace.vercel.app. כל push ל-`main` ב-GitHub (`avivalushai/nehorace`) מתפרסם אוטומטית ב-Vercel תוך פחות מדקה. אחרי העלאה מריצים `npm run test:live` בתיקייה `tests`.

## הרצה

```bash
node tests/dev-server.mjs   # ואז http://localhost:8765
```
שרת הפיתוח לא דורש התקנה. הוא מגיש את המשחק, מריץ את פונקציות השרת שב-`api/`, ומדמה את מסד הנתונים בזיכרון, כך שגם טבלת האלופים עובדת מקומית (והנתונים נמחקים כשהשרת נעצר). אפשר גם `python3 -m http.server 8000`, אבל אז טבלת האלופים לא תעבוד. פתיחה ישירה של `index.html` מהקובץ לא עובדת, כי הדפדפן חוסם מודולים מ-`file://`.

## מסכי המשחק

1. פתיחה: שם הדמות. כולם "נהוראי", והשחקן מוסיף מילה משלו ("נהוראי המלך"). השם ייחודי, נשמר בדפדפן ומוצג בטבלת האלופים.
2. בניית נהוראי: תשע קטגוריות פריטים (הלשוניות נשברות לשתי שורות בטלפון כדי שכולן ייראו), הדמות תמיד מוצגת למעלה.
3. בחירת כלי: קורקינט חשמלי, אופניים חשמליים, טרקטורון.
4. עיצוב הכלי: צבע, גלגלים, מדבקות.
5. המירוץ: מבט על, שלושה אזורים, הולכי רגל, פעילויות על הדשא, בועות דיבור. במחשב מוצג רמז למקשים.
6. סיכום: נקודות, מטבעות (עם פירוט מה דרסת), כפתור "שלח בוואטסה" (עם שגיאת הכתיב, בכוונה), אלבום מגנטים, טבלת מירוץ, חנות, ו"המוסך" עם כל מה שהשחקן השיג (רק מה שנפתח או נקנה, בלי פריטים נעולים).

## מערכות עיקריות

- **ציור דמויות:** `drawNeho` ומשפחת הפריטים (שיער, זקן, כובע, שרשרת, כלב). הדמות נבנית מלבנים עם קו מתאר.
- **ציור כלים:** `drawVehicleSide`, `drawVehicleFront`, `drawVehicleRear`, `drawWheel`.
- **מנוע המירוץ:** `startRace`, `update`, `render`, `genWorld`, יחד עם `knock`, `hitStatic` ו-`say`.
- **מוזיקה:** מנוע מזרחי מבוסס `WebAudio` שמייצר תופים, בס ומלודיה, וכן נגן לשירים שהמשתמש טוען מהמכשיר.
- **אלבום "מתנה קטנה מאירוע גדול":** 10 מגנטים מהמירוץ מתוך 23 סוגי תמונות, כל אחת פונקציית `scene*`, עם פלטות, פילטרים ומסגרות. כל מגנט אפשר לשלוח בוואטסאפ.
- **שיתוף בוואטסאפ:** `src/ui/share.js`. בטלפון נפתח חלון השיתוף של המכשיר עם תמונה שמצוירת בקוד (כרטיס המירוץ עם הנהוראי של השחקן, או המגנט) וטקסט עם הקישור. במקום שאי אפשר לשתף תמונה (רוב המחשבים) נפתח `wa.me` עם הטקסט והקישור.
- **שיאים:** `src/core/stats.js` שומר ב-`localStorage` (`nehorace-stats`) מירוצים, ניצחונות, שיא נקודות וזמן הכי מהיר. מוצג במסך הפתיחה, ו"שיא חדש" מוצג במסך התוצאות.
- **פריטים שנפתחים:** כל קטגוריה בבניית הנהוראי כוללת לפחות 10 פריטים. החדשים נפתחים לפי נקודות קריירה, יש 3 כלים שנפתחים (טי-מקס, פיטבול ענק, כנפי השכינה), והכל פתוח למי שבחר קודם. פרטים ב-`docs/ARCHITECTURE.md`. במצב `?dev` יש כפתור שמוסיף 5,000 נקודות.
- **טבלת האלופים:** פונקציות שרת ב-`api/` (Vercel) ו-Upstash Redis דרך fetch, בלי ספריות. שחקן מזוהה במזהה אקראי ששמור בדפדפן (`nehorace-player`), בלי התחברות, ומופיע בשם הנהוראי שלו ועם הנהוראי שבנה (בפודיום ובכל שורה). יש טבלה שבועית (המירוץ הכי טוב, מתאפס ביום ראשון לפי שעון ישראל) וטבלת ניצחונות של כל הזמנים. פרטים ב-`docs/ARCHITECTURE.md`.
- **אנליטיקה (Amplitude):** `src/net/analytics.js` שולח אירועים ב-fetch ל-HTTP API של Amplitude (מרכז הנתונים באירופה), בלי SDK. השחקן מזוהה באותו מזהה אקראי של טבלת האלופים, ולא נשלחים שמות. אירועים נשלחים רק מהאתר החי, אף פעם לא מ-localhost, מ-`?dev` או מדפדפן אוטומטי (הבדיקות). בכל מקום אחר הם נשמרים רק ב-`window.__amp`, והבדיקה בודקת אותם שם. קישור ששותף בוואטסאפ מסתיים ב-`?from=wa`. האירועים:
  - `game_opened`: כניסה למשחק (`returning`, `has_saved_name`, `device`, `from`).
  - `build_nehorai_clicked`: "בונים נהוראי" במסך הפתיחה (`result`: `ok`, `no_name` או `name_taken`).
  - `title_leaderboard_clicked`: טבלת האלופים במסך הפתיחה.
  - `choose_vehicle_clicked`: "לבחירת כלי" (`changes` בשלב, וכל פריטי הנהוראי).
  - `design_vehicle_clicked`: "לעיצוב הכלי" (`changes`, `vehicle`).
  - `start_race_clicked`: "יאללה למירוץ" (`changes`, `vehicle`, `color`, `wheels`, `stickers`).
  - `race_finished`: סיום מירוץ (`position`, `score`, `race_time`, `victims`, `coins`, `vehicle`, `career_points`).
  - `results_button_clicked`: כל כפתור במסך התוצאות (`button`).
- **כלכלה:** `calcCoins`, `renderShop`, `buyItem`. הארנק (מטבעות וקניות) נשמר ב-`localStorage` תחת המפתח `nehorace-wallet`, ו-`window.storage` של Claude משמש רק כגיבוי. הנתונים נבדקים בטעינה, כך שמידע פגום מתאפס לארנק ריק ולא שובר את המשחק.

## כללי עבודה

- **מודולים:** משתנה `let` שמיוצא ממודול אחד אי אפשר לשנות ממודול אחר (import הוא לקריאה בלבד). כשצריך לשנות אותו מבחוץ, מוסיפים פונקציית setter במודול שמחזיק אותו, כמו `setStep` ב-`ui/garage.js` ו-`resetPid` ב-`race/world.js`.
- **סדר טעינה:** קוד שרץ בזמן טעינת מודול (לא בתוך פונקציה) לא יכול לקרוא `const` ממודול שעוד לא נטען. אתחול המשחק נשאר בסוף `src/main.js`.

- **עברית בלבד בממשק.** כל הטקסטים בעברית, `dir="rtl"` על המסמך. מספרים בתוך טקסט עברי צריכים `direction:ltr` ו-`unicode-bidi:isolate` כדי לא להתהפך.
- **טלפון קודם.** כל מסך חייב לעבוד במסך של 390 על 844 בלי גלילה אופקית, ועם אזורי נגיעה גדולים.
- **בלי ספריות.** אין `npm`, אין `React`, הכל וניל.
- **ציור בקוד.** כל ויזואל חדש נבנה מצורות קנבס, לא מתמונות.
- **טון הומוריסטי.** הקללות והכיתובים הם חלק מהמשחק, בסגנון רחוב ישראלי, בלי גסויות קיצוניות ובלי פגיעה בקבוצות.

## עיצוב

```
--club:#2E1447  --club2:#431F66  --gold:#FFC83D  --gold2:#D9951A
--pink:#FF3D8B  --mint:#3DDC97   --cream:#FFF4DC  --ink:#1A0B29
```
- כותרות: `Karantina` (משתנה `--display`). טקסט: `Secular One` (משתנה `--ui`). שניהם נטענים מ-`Google Fonts` עם נפילה לפונט מערכת.
- כפתורים: פינות עגולות מלאות, צל תחתון קשיח, לחיצה מזיזה 3 פיקסלים למטה.
- הקנבס מצייר עם קו מתאר כהה בעובי 3 ומפרקים מעוגלים.

## בדיקות

**קיצור לבדיקות:** מוסיפים `?dev` לכתובת (למשל http://localhost:8765/?dev). במסך הפתיחה יופיע כפתור שמריץ מירוץ שלם בהדמיה ועובר ישר למסך התוצאות. בלי `?dev` הכפתור לא מוצג לשחקנים.

בדיקת `Playwright` נמצאת ב-`tests/smoke.mjs`. זה כלי פיתוח בלבד, והמשחק עצמו נשאר בלי תלויות. התקנה חד-פעמית:

```bash
cd tests && npm install && npx playwright install chromium-headless-shell
```

הרצה (מתוך `tests/`):

- `npm run test:quick`: עוברת על כל מסכי המוסך ועל תחילת המירוץ, בערך 40 שניות. להריץ אחרי כל שינוי.
- `npm test`: מירוץ שלם, תוצאות, חנות ואלבום, בערך דקה וחצי. להריץ לפני מסירה.
- `npm run images`: מייצרת מחדש את האייקונים ואת `assets/share.jpg` מ-`src/art/brand.js`.
- `npm run test:update`: מעדכנת את `tests/baseline/` אחרי שינוי עיצוב מכוון.

הבדיקה מרימה שרת בעצמה ועובדת במסך 390 על 844. היא בודקת גם שהאייקונים, תמונת השיתוף וה-manifest נטענים בגודל הנכון, שהארנק נשמר אחרי רענון ושהמשחק שורד מידע פגום בארנק. היא נכשלת אם יש שגיאות ב-`console`, גלילה אופקית, או מסך סטטי שלא זהה פיקסל אחר פיקסל ל-`tests/baseline/`. צילומי המסך נשמרים ב-`tests/output/`. כשמשנים עיצוב בכוונה, בודקים את הצילומים ב-`tests/output/`, ורק אז מעדכנים את ה-baseline.
